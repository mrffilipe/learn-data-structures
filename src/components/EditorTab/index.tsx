import styles from './styles.module.css'

import Sidebar from '../Sidebar'

type Props = {}

const EditorTab = (props: Props) => {
    return (
        <div>
            <Sidebar />
        </div>
    )
}

export default EditorTab